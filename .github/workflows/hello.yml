name: My First Workflow

on:
  push:
    branches: [main]

jobs:
  hello:
    runs-on: ubuntu-latest

    steps:
      - name: 1 step
        run: echo "안녕하세요"

      - name: 2 step 현재 시간과 날짜
        run: date

      - name: 3 step
        run: echo "${{github.actor}} 님"